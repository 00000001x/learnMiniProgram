<!--pages/wxs/wxs.wxml-->
<!--1.错误的写法-->
<!-- <view>{{25.66666.toFixed(2)}}</view> -->

<!--2.wxs的定义方式-->
<!--2.1直接在wxml中定义-->
<!-- <wxs module="info">
//Js代码
var message = "Hello World";
var name = "sky";
function sum(num1,num2){
  return num1+num2;
}

//commonjs的模块化导出的写法
module.exports = {
  message:message,
  name:name,
  sum1:sum
}
</wxs> -->

<!--2.2 定义在单独的wxs文件中，再使用wxs进行导入-->
<!--注意：不允许使用绝对路径，得使用相对路径-->
<wxs src="../../wxs/infoj.wxs" module="info"/>
<view>{{info.message}}</view>
<view>{{info.name}}</view>
<view>{{info.sum1(1,1)}}</view>

<!--3.wxs的应用-->
<wxs src="../../wxs/format.wxs" module="format"/>
<view>{{format.priceFormat(3.1315926,3)}}</view>
<view>{{format.dateFormat(time,'yyyy-MM-dd hh:mm:ss')}}</view>
